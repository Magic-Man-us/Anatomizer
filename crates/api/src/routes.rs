// Additional route handlers will be added here as the API grows.
// For now, the main analyze endpoint lives in main.rs.
//
// Planned routes:
// - POST /analyze          — full analysis (implemented in main.rs)
// - POST /parse            — parse only (AST)
// - POST /disassemble      — assembly/bytecode only
// - GET  /languages        — list supported languages
// - GET  /health           — health check
